  hue:
    container_name: hue
    image: gethue/hue:latest
    ports:
      - "8889:8888"  # Hue web interface
    environment:
      - HUE_DATABASE_ENGINE=sqlite3
      - HUE_DATABASE_NAME=hue
      - HUE_DATABASE_USER=hue
      - HUE_DATABASE_PASSWORD=hue_password
    volumes:
      - shared-workspace:/home/jovyan/work
    depends_on:
      - spark-master
      - hive-metastore
    networks:
      - my-network