  dagster:
    container_name: dagster
    build:
      context: ./docker/dagster
      args: 
        IMAGE: ${IF_USING_LOCAL_REGISTRY}python:3.9-slim
    ports:
      - "3000:3000"
    depends_on:
      - postgres
    networks:
      - my-network
    # volumes:
    #   - ./dagster:/opt/dagster
    #   - ./pyflink:/opt/pyflink