your_profile_name:
  target: prod
  outputs:
    dev:
      type: spark
      method: thrift
      host: spark-thrift-server
      port: 10000
      schema: dev_schema
      user: spark_user
      password: spark_password
      database: dev_database
    prod:
      type: spark
      method: thrift
      host: spark-thrift-server
      port: 10000
      schema: prod_schema
      user: spark_user
      password: spark_password
      database: prod_database
