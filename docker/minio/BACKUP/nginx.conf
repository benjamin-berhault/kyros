# Main configuration file for Nginx

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    # Include the server configuration from conf.d/default.conf
    include /etc/nginx/conf.d/*.conf;
}
