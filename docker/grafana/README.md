# Grafana Docker Image with Custom Dashboards and Datasources

## Dockerfile Overview

The Dockerfile starts from a base Grafana image and adds custom provisioning configurations for dashboards and datasources.

## Files Description
- `dashboard.yml`: Configuration file for provisioning Grafana dashboards.
- `datasource.yml`: Configuration file for provisioning Grafana datasources.
